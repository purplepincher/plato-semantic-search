{
  "name": "plato-semantic-search",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "test": "npx mocha test/**/*.test.ts",
    "batch:upsert": "ts-node scripts/batch-ingest.ts /batch/upsert",
    "batch:delete": "ts-node scripts/batch-ingest.ts /batch/delete",
    "ingest": "ts-node scripts/batch-ingest.ts /ingest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "@types/node": "^20.11.0",
    "wrangler": "^3.57.0"
  }
}
