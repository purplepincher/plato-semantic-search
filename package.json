{
  "name": "plato-semantic-search",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "seed": "tsx scripts/seed.ts",
    "seed:dev": "PLATO_BASE_URL=http://localhost:8787 tsx scripts/seed.ts",
    "seed:dry-run": "tsx scripts/seed.ts --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.2",
    "vitest": "^1.6.0",
    "wrangler": "^3.78.0"
  }
}
