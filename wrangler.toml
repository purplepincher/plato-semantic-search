name = "plato-semantic-search"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[ai]
binding = "AI"

[[vectorize]]
binding = "VECTORIZE"
index_name = "plato-search"

# Secrets (set via: wrangler secret put API_KEY)
# API_KEY — optional Bearer token protecting every endpoint (incl. /health)
